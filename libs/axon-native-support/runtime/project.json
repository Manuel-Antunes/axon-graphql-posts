{
  "name": "dev.manuelantunes:axon-native-support",
  "targets": {
    "publish-local": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["^publish-local"],
      "inputs": ["default", "^default"],
      "outputs": [],
      "options": {
        "forwardAllArgs": false,
        "command": "./mvnw install -DskipTests -q -pl libs/axon-native-support/runtime,libs/axon-native-support/deployment"
      }
    }
  }
}
