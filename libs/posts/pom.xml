<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.manuelantunes</groupId>
        <artifactId>quarkus-axon-graphql</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>axonposts-posts</artifactId>
    <name>posts</name>
    <description>O módulo de posts: os agregados Post e Tag, com domínio, aplicação e persistência</description>

    <!--
      MÓDULO DE POSTS — DOIS AGREGADOS, UM MÓDULO
      ===========================================
      `Post` e `Tag` são ambos `@EventSourcedEntity`, ou seja dois agregados. Vivem no mesmo módulo
      porque `Tag` só existe para servir `Post`: não há caso de uso que crie ou consulte tag fora do
      contexto de um post, e a tag padrão é criada por um handler de evento de post. Módulo é fronteira
      de coesão, não sinônimo de agregado.

      Depende de `users` porque `Post.author` é um `User` — dependência real do domínio, não acidente
      de código. A recíproca não existe.
    -->
    <dependencies>
        <dependency>
            <groupId>dev.manuelantunes</groupId>
            <artifactId>axonposts-platform</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm</artifactId>
        </dependency>

        <dependency>
            <groupId>dev.manuelantunes</groupId>
            <artifactId>axonposts-users</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-eventsourcing</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- Sem o índice, bean CDI e entidade JPA desta lib são invisíveis. Ver libs/platform. -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- MapStruct: o `defaultComponentModel=jakarta-cdi` vem do parent e vale aqui também -->
        </plugins>
    </build>
</project>
